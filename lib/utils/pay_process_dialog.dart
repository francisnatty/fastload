import 'package:flutter/material.dart';

class CustomPaymentAlert extends StatelessWidget {
  const CustomPaymentAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Container(),
    );
  }
}
